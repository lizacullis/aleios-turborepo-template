{
  "name": "@myproject/dummy-package",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint \"**/*.{ts,js,tests.ts}\"",
    "unit-tests": "jest --testPathPattern=./tests/unit-tests --coverage",
    "integration-tests": "jest --testPathPattern=./tests/integration-tests --coverage"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "18.19.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "5.3.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}
